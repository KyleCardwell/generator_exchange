import { brandColors } from "@/constants/colors";

export const sectionClasses = "mx-auto w-full max-w-6xl px-6 py-8";
export const cardClasses = "rounded-2xl border border-slate-200 bg-white p-6 shadow-sm";
export const inputClasses =
  "w-full rounded-lg border border-slate-300 bg-white px-3 py-2 outline-none focus:border-slate-500";

export const brandStyles = {
  heroBackground: {
    backgroundColor: brandColors.primary,
  },
  footerBackground: {
    backgroundColor: brandColors.primary,
  },
  primaryButton: {
    backgroundColor: brandColors.accent,
  },
};
